# Long-running STARK prover integration tests (`#[ignore]` in tests/prover.rs).
name: Prover (weekly)

on:
  schedule:
  # Sunday 06:00 UTC
    - cron: "0 6 * * 0"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  TRACY_NO_INVARIANT_CHECK: "1"
  NNS_KERNEL_JAM: ${{ github.workspace }}/nns/nns.jam

jobs:
  prover:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    defaults:
      run:
        working-directory: nns

    steps:
      - name: Checkout nns
        uses: actions/checkout@v4
        with:
          path: nns

      - name: Install Rust (nightly)
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: nightly

      - name: Install protoc
        uses: arduino/setup-protoc@v3

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: nns
          cache-on-failure: true

      - name: Install hoonc and nockup
        run: |
          cargo +nightly install --git https://github.com/nocktoshi/nockchain.git --branch dev --locked -p hoonc -p nockup

      - name: Build kernel jam
        run: make install-kernel DESTDIR=$HOME/.local

      - name: Prover tests (ignored)
        run: cargo +nightly test --test prover -- --ignored --nocapture
